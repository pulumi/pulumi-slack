provider: slack
major-version: 1
esc:
  enabled: true
env:
  SLACK_TOKEN: ${{ secrets.SLACK_TOKEN }}
lint: true
providerDefaultBranch: main
makeTemplate: bridged
plugins:
  - name: terraform
    version: "1.0.16"
    kind: converter

  - name: random
    version: "4.3.1"
team: ecosystem
pulumiConvert: 1
registryDocs: true

